module.exports = (function(window) {
    const updateHost = 'http://pub.dmp.fang.com';
    const config = {
        urls: {
            API_FANG_PUB: updateHost
        }
    };
    return config;
}(window));